flask==2.3.2
# Pin Werkzeug to the 2.3.x line; Flask 2.3.2 is incompatible with Werkzeug 3.x.
werkzeug==2.3.7
flask-cors==4.0.0
# Rate limiting (brute-force / spam protection). 3.5.x supports Flask 2.3.
Flask-Limiter==3.5.0
gunicorn==21.2.0
sqlalchemy==2.0.23
alembic==1.13.2
pydantic==2.5.0
python-dotenv==1.0.0
PyYAML==6.0.2
# Algorand on-chain layer (Phase 3). The app runs without a configured treasury
# (falls back to an offline mock chain), but this is required to mint on TestNet.
py-algorand-sdk==2.11.1
# AI "Master Grower" advisor. The app runs without an ANTHROPIC_API_KEY (falls
# back to an offline mock advisor); this is required for real Claude advice.
anthropic>=0.49.0
# HTTP client used by the ElevenLabs TTS narrator.
requests>=2.31.0
# Postgres driver for production (Render). Local/dev/test default to SQLite and
# do not require this; safe to skip if the binary wheel is unavailable.
psycopg2-binary==2.9.9
alembic==1.13.2
anthropic
flask==2.3.2
flask-cors==4.0.0
Flask-Limiter==3.5.0
gunicorn==21.2.0
psycopg2-binary==2.9.9
py-algorand-sdk==2.11.1
pydantic==2.5.0
python-dotenv==1.0.0
PyYAML==6.0.2
sqlalchemy==2.0.23
werkzeug==2.3.7
