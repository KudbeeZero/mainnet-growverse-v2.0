// Human-visible build marker so the owner can confirm at a glance which update
// is live after a hard refresh. Bump this with each shipped UI update.
//   2.0   — mainnet baseline
//   2.0.1 — guided onboarding + token-claim banner (this update)
export const APP_VERSION = "2.0.1";
