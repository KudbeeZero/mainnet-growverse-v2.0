# dependencies
/node_modules
/.pnp
.pnp.js
# npm is canonical here (see CLAUDE.md); ignore env-specific pnpm lockfiles
# generated when installing against a public registry off-Replit.
pnpm-lock.yaml

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# typescript
*.tsbuildinfo

# test coverage
/coverage

# playwright
/test-results
/playwright-report
/playwright/.cache
/e2e-output

# canonical stage PNGs (regenerable build artifacts: `npm run gen:stages`)
/canonical-stages

# Agent scratch helpers (screenshot/count probes) — never committed
_*.mjs
_*.mts
.next-verify/
