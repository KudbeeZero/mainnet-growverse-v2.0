# LUT round-table — 2026-06-14 (BE-004, STEP 4: e2e Grow Loop)

**Directive:** BE-004 · **Lead:** BE-A00 · **Branch:** `claude/simulation-test-clock-u4ounm` (extends PR #47)

## Asked
Validate the full grow loop end-to-end (seed → plant → grow → flower → harvest → sell) over the HTTP
API using the STEP 3 dev clock, plus HTTP-boundary coverage for the value-bearing routes (RISK #8) —
test-only, no economy/production changes.

## Done
- **`tests/test_e2e_grow_loop.py` (3)** — full loop over HTTP, fast-forwarded via
  `POST /api/dev/clock/advance`: seed→plant (asserts `seed`), care-loop to **flowering**, **harvest**
  (server-side yield), **sell** (balance == `harvest_sale` ledger entry); no-double-sell; and
  **BE-A08** ledger integrity (advancing time posts zero ledger entries).
- **`tests/test_http_boundary.py` (13)** — withdraw/deposit (happy + validation + auth + insufficient),
  mint harvest (happy/idempotent/not-found), mint strain non-breeder, ARC-3 metadata + unknown kind.
- **Reports:** `docs/STEP4_E2E_GROW_LOOP_VALIDATION.md` (validation + risks + recommendations);
  `docs/audits/PR-47-simulation-test-clock.md` (handoff-audit of STEP 3 → PASS).

## Gates
- `make test` → **262 passed** (+16), coverage **83.63% ≥ 79** ✅ · `make lint` ✅ · `make check-memory` ✅ (22 files).

## Risks / findings
- **NEW (MED, deferred by owner):** `GameService` defaults to `SystemClock`, not `active_clock()`
  (`game_service.py:82`) → dev clock can't fast-forward **cure**/auction over HTTP. One-line fix,
  prod-identical; next chat (STEP 4.5).
- **Carried:** RISK #8 backend HTTP boundary now covered; **web** side (Playwright real e2e,
  treasury-cap/rollback UI) still open. PR #47 had **no GitHub CI run on its head** — gates re-verified
  locally; CI runs on this push.

## Needs owner (by hand)
`GROW_TEST_CLOCK=true APP_ENV=development make serve`; `POST /api/dev/clock/advance {"days":40}` →
plant flowers on next `/state`; harvest + sell succeed; `/api/dev/clock/*` 404 with flags unset.

## Next
**STEP 4.5** — `GameService` → `active_clock()` one-liner + cure/auction e2e (owner-approved).
