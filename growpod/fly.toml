# Fly.io configuration for the FRONTIER backend (GROWv2 / growpodempire) — the
# Flask API only. The public game UI is the separate Next.js app in web/, served
# via Cloudflare Pages at https://frontierprotocol.app. This API is meant to live
# behind https://api.frontierprotocol.app. Deploy from this directory (growpod/).
# See docs/DEPLOY_FLY.md for the full runbook (secrets, certs, DNS).
app = "frontiernext"
primary_region = "ord"

[build]
  dockerfile = "Dockerfile"

[env]
  # Fly routes to internal_port below; gunicorn binds 0.0.0.0:$PORT.
  PORT = "8080"
  # src/ layout: make the growpodempire package importable (mirrors render.yaml).
  PYTHONPATH = "src"

# Run schema migrations + the idempotent strain seed before each new version goes
# live, mirroring the existing Render preDeployCommand. This runs in a temporary
# machine and REQUIRES the DATABASE_URL secret to be set first
# (`fly secrets set DATABASE_URL=... -a frontiernext`). If you prefer to run
# migrations by hand, comment this block out.
[deploy]
  release_command = "alembic upgrade head && python -m growpodempire.db.seed"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  # Background threads (seasonal rollover, audio prewarm) catch up on cold start,
  # so scale-to-zero is safe for early deploys. Raise to 1 to keep them warm.
  min_machines_running = 0

  # Real liveness route defined in src/growpodempire/api/observability.py.
  # (/readiness also exists and pings the DB if you want a deeper check.)
  [[http_service.checks]]
    method = "GET"
    path = "/health"
    interval = "30s"
    timeout = "5s"
    grace_period = "10s"

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"
