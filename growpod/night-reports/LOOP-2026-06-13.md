# Night loop + crew evaluation — 2026-06-13

Autonomous launch-prep session (owner away, granted permission). Everything landed on
**PR #22** (`claude/deflake-forecast-test`) per "keep everything in one PR." All gates run green
before each push.

## Landed on #22 (in order)
1. **De-flake** `test_service_forecast_uses_its_clock` (+ sibling) — root cause was an early random
   pest spawn stretching the seed stage; fixed with a 5-day window. Honest comments, no dead code.
   25/25 deterministic; full suite 223.
2. **C1 — per-plant seed (real bug):** chamber passed `seed={plantId.length}`; all UUIDs are 32 chars
   → every plant rendered identically. Now `seedForPlant(plantId)`.
3. **De-gloss → matte resin** (owner: "too shiny/plasticky"): removed the white specular ellipse on
   calyxes, dimmed highlight slivers + trichome glints, switched frost from additive (`lighter`/`screen`)
   to normal-blend cool cream-grey, softened the fan midrib.
4. **P1 — hero visibility-gate:** IntersectionObserver pauses the strain-lab hero RAF loop when offscreen.
5. **#8 (partial) — vitest in web CI:** harness/config/5 test files already existed but vitest was never
   installed and the `test` script was a stub. Added vitest, wired `test` → `vitest run`, added a CI step.
   **100 unit tests now run on every PR.**
6. **Renderer hardening:** `drawFan` clamps to the FAN table length (future >9-leaflet fans can't NaN);
   removed a dead `branchFlex` local.

## Night-crew evaluation (independent reviewers) — `main` is launch-ready off-chain
- **Money/economy: PASS.** Race-safe (wallet + listing optimistic locks, `CHECK ≥ 0`, unique harvest
  index, Decimal throughout; 20/20 tests). PR #21's auction lock verified correct/atomic.
- **API/security: PASS in scope.** No IDOR (all 40 player-mutations gated), SpendGuard unescapable,
  validation 500s→400, `StaleDataError`→409 app-wide; 24/24 tests.
- **Renderer: CONCERNS → C1 + P1 fixed here.** Plant-look specialist gave a ranked de-grape/de-gloss/
  leaf/stacking plan (see `knowledge/whole-plant-architecture.md`); de-gloss done, rest is graphics phase.

## Carried-risk status
- **#7 chain settlement — OPEN, launch gate.** Withdraw/deposit non-idempotent/non-atomic + deposit
  trusts no on-chain proof. STOP-AND-ASK (real money/treasury). Launch off-chain with it disabled.
- **#8 web safety net — PARTIAL (improved).** Vitest now runs in CI (this session). Remaining: Playwright
  e2e, treasury-cap + chain-failure-rollback tests.
- **#10** web 401/403 handler, **#11** `get_level` public oracle + rate-limit Redis — OPEN, minor.

## Queued / needs owner decision (NOT done autonomously)
- **Graphics phase** (taste — changes all strains' looks): pointed overlapping bracts (de-grape),
  denser fan/sugar leaves + per-strain leaf morphology, vertical cola stacking. Owner offered stage
  reference stills — those slot in here.
- **White Rhino** — full spec captured; queued as the final strain to add at mainnet go-live.
- **Backend nits:** reads returning 400-instead-of-404 on not-found (needs a NotFoundError type; risks
  suite); faucet uniqueness index (migration + money path); `get_level` gating (is it intended public?).
- **#7 chain hardening** — design only until owner provides funded TestNet + keys.

## To merge #22
CI green on the head; supersedes #20. It's a clean "launch cleanup" PR (de-flake + render fixes +
vitest-in-CI). Owner to merge when satisfied.
